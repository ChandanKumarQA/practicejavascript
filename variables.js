let name = "Chandan";  // can be changed
const role = "QA";     // cannot be changed

name = "Chandan Kumar"; // ✅ works
// role = "Developer";  ❌ error, const cannot be reassigned

console.log(name, role);
